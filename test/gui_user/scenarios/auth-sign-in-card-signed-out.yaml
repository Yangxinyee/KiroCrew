# Smoke: the Kiro sign-in card on Settings > Overview in its signed-out state.
# The seeded home holds no Kiro identity and the target has no network, so the
# card's status probe answers "not signed in" deterministically and the card
# offers the provider buttons. Completing a sign-in needs a real external
# identity and is out of scope; this asserts that a user who cannot start a
# session can see WHY (no identity) and WHERE to fix it (the card's buttons).
name: auth-sign-in-card-signed-out
tier: smoke
feature: auth
user_story: >-
  As an operator whose agents will not start, I want Settings to show me
  whether Kiro Crew holds a Kiro identity and how to sign in, so that I can
  fix the missing sign-in without reading logs.
docs_url: docs/system-specs/modules/kas-auth.md
summary: Open Settings > Overview and read the signed-out Kiro sign-in card
preconditions:
  seed: rich
  members: [nova-sky]
  start_url: /settings/overview
steps:
  - You are on the Overview tab of Settings. Scroll until you find a card titled "Kiro sign-in" (it has a key icon).
  - Wait two seconds so the card can finish checking the sign-in status (a short "Checking sign-in…" line may show first).
expectations:
  - The "Kiro sign-in" card is visible and no longer says "Checking sign-in…".
  - The card does not say "Signed in with"; instead it offers at least one sign-in button whose label starts with "Continue with" (for example "Continue with GitHub" or "Continue with AWS Builder ID").
  - No error banner is shown on the card.
max_steps: 8
max_seconds: 240
